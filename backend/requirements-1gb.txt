# Lightweight profile for 1GB RAM devices
Flask==3.0.3
pyserial==3.5
openpyxl==3.1.5
RPi.GPIO==0.7.1

# NOTE:
# face-recognition and opencv are excluded for 1GB mode.
# Run with FACE_REQUIRED=false when using this profile.
